# Vite client (scaffold)

This folder contains a minimal Vite + React scaffold to start porting the Next.js frontend.

How to run:

```bash
cd vite-app
npm install
npm run dev
```

Notes:
- Components are ported from the Next app but need routing and API integration work.
- Keep the Next app API (`/api/*`) or create a separate backend to serve the same endpoints.
